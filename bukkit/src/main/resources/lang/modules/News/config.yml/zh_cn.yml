module-enabled: false
book-news:
  # 此服务器的新闻频道
  # 所有新闻数据均存储在数据库中，因此如果您有多个相同的服务器，可以使用同一频道
  channel: main
  show-unread-news-on-join: true

  # 创建新新闻时使用的默认布局
  new-layout:
    default: |-
      <pdc><b>Title </b> 
      <pc>Body

      <tc><time:'yyyy-MM-dd HH:mm'>
